// Environment configuration template for QuickBite
// Copy this to env.js and fill in your production URLs
// For Vercel deployment, set environment variables in Vercel dashboard

window.env = {
    // API URL - use localhost for local development
    // For production on Vercel: set VITE_API_URL environment variable
    // OR update the fallback URL below to your backend URL
    API_URL: 'http://localhost:8080/api'
};
