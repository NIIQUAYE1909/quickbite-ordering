// Runtime environment configuration for QuickBite.
// Update API_URL after the backend is deployed.
window.env = Object.assign({}, window.env, {
    API_URL: 'https://your-render-service.onrender.com/api'
});
